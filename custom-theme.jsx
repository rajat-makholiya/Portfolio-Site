import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    h1: {
      fontFamily: "Knewave",
      fontWeight: 500,
    },
    subtitle1: {
      fontFamily: "Segoe UI",
    },
    h5: {
      fontFamily: "Segoe UI",
    },
  },
});

export default theme;